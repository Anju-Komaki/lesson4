<h1>Books</h1>

<h2>New book</h2>
<%= form_for(@book) do |f| %>
	<%= f.label :title, "Title" %><br>
	<%= f.text_field :title %><br>
	<%= f.label :title, "Body" %><br>
	<%= f.text_area :body %><br>
	<%= f.submit 'Create Book' %>

<% end %>

