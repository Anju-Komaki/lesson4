<!DOCTYPE html>
<html>
  <head>
    <title>TravelApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/c7b8ec43cf.js" crossorigin="anonymous"></script>
  </head>
    <body>
<header class="navbar navbar-fixed-top navbar-inverse">
    <div class="container">
      <nav class="nav navbar-nav navbar-left">
        <span class="bookers">Travel App</span>
      </nav>
      <nav>
    <ul class="nav navbar-nav navbar-right" data-hover="dropdown" data-animations="zoomIn fadeIn fadeInUp fadeIn">
    <% if user_signed_in? %>
    <li><%= link_to 'Record', user_path(current_user.id), class: "glyphicon glyphicon-picture" %></li>
    <li><%= link_to 'Write', new_travel_image_path, class: "glyphicon glyphicon-pencil" %></li>
    <li><%= link_to 'Users', users_path, class: "glyphicon glyphicon-user" %></li>
    <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-new-window" %></li>
    <% else %>
      <li><%= link_to "About", about_path, class: "glyphicon glyphicon-info-sign" %></li>
      <li><%= link_to "SignUp", new_user_registration_path, class: "glyphicon glyphicon-link" %></li>
      <li><%= link_to "Login", new_user_session_path, class: "glyphicon glyphicon-check" %></li>
    <% end %>
  </ul>
</nav>
</div>
</header>
<br><br>
    <div class="container">

     <%= yield %>
      </div>
    <footer>
    <div class="container">
      <div class="row">
        <div class="col-xs-3 col-xs-offset-9">
          <p class="pull-right">CopyRight Infratop.inc</p>
        </div>
      </div>
    </div>
  </footer>
    </body>
</html>
